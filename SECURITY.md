# 安全政策

## 支持范围

安全修复面向默认分支和最新公开 Release。旧 Release 仅在问题影响读者安全且维护成本可控时回补；教材示例和离线参考实现不是托管服务，也不提供商业 SLA。

## 私下报告漏洞

请不要在公开 Issue 中提交未修复的漏洞、真实密钥、个人数据或可直接利用的攻击细节。优先使用仓库的 [GitHub Private Vulnerability Reporting](https://github.com/wujinjun/ai-agent-book/security/advisories/new)。如果该入口不可用，可先创建不含敏感细节的普通 Issue，请维护者提供私下沟通方式。

报告至少包含：

- 受影响的提交、Release、章节、示例或项目；
- 环境与精确依赖版本；
- 最小复现、影响与攻击前提；
- 已做的脱敏以及建议修复；
- 是否计划公开披露及期望时间。

维护者会尽力在 7 天内确认是否收到，并在完成复现后说明严重性、修复计划或不接受原因；这不是合同性响应承诺。修复发布前，请避免公开利用细节。

## 安全边界

以下情况通常不作为仓库漏洞：需要读者主动把真实密钥写进示例、只影响已明确不支持的 Python 版本、没有安全影响的文案错误、第三方服务自身漏洞，以及未提供最小复现的自动扫描噪声。仍欢迎通过合适模板报告内容风险或依赖弃用。

仓库默认要求最小权限、显式审批、超时、有限重试、租户隔离、脱敏 Trace 和密钥环境变量。通过测试不表示系统已适合生产；接入真实身份、支付、邮件、股票、文件或浏览器写操作前必须重新执行威胁建模和组织安全评审。
